<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Cheese Article</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">

    <h2 class="mb-4">Edit Cheese Article</h2>
    
    <form action="/articles/update/<%= article.id %>" method="POST">
        
        <input type="hidden" name="_method" value="PUT">

        <!-- Hidden Feedback ID Input -->
        <div class="mb-3" hidden>
            <label class="form-label">Feedback ID</label>
            <input type="text" name="id" class="form-control" id="feedbackId" value="<%= article.id %>" readonly>
        </div>

        <!-- Title Input -->
        <div class="mb-3">
            <label for="title" class="form-label">Title</label>
            <input type="text" class="form-control" id="title" name="title" value="<%= article.title %>" required>
        </div>

        <!-- Hidden CreatedAt Field -->
        <input type="hidden" name="createdAt" value="<%= article.createdAt %>">

         <!-- Image URL Input -->
         <div class="mb-3">
            <label for="imageUrl" class="form-label">Image URL</label>
            <input type="url" class="form-control" id="imageUrl" name="imageUrl" placeholder="Enter image URL..." required>
        </div>

        <!-- Topic Radio Buttons -->
        <div class="mb-3">
            <label class="form-label">Topic</label>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="topic" id="softCheese" value="Soft Cheese" <%= article.topic === "Soft Cheese" ? 'checked' : '' %> required>
                <label class="form-check-label" for="softCheese">Soft Cheese</label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="topic" id="hardCheese" value="Hard Cheese" <%= article.topic === "Hard Cheese" ? 'checked' : '' %>>
                <label class="form-check-label" for="hardCheese">Hard Cheese</label>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="radio" name="topic" id="general" value="General" <%= article.topic === "General" ? 'checked' : '' %>>
                <label class="form-check-label" for="general">General</label>
            </div>
        </div>

        <!-- Description Text Area -->
        <div class="mb-3">
            <label for="description" class="form-label">Description</label>
            <textarea class="form-control" id="description" name="description" rows="3"><%= article.description %></textarea>
        </div>

        <!-- Article Text Area -->
        <div class="mb-3">
            <label for="article" class="form-label">Article</label>
            <textarea class="form-control" id="article" name="article" rows="6"><%= article.article %></textarea>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">Update Article</button>

    </form>

    <script>
        // Optional: If you need dynamic behavior, you can add JS logic
    </script>

</body>
</html>
