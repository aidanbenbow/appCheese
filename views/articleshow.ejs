<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= article.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container mt-5">

    <div class="card shadow-lg">
        <div class="card-body">
            <h1 class="card-title"><%= article.title %></h1>
            <h6 class="text-muted">Created At: <%= new Date(article.createdAt).toLocaleDateString() %></h6>
            <hr>

             <!-- Display Article Image -->
    <% if (article.imageUrl) { %>
        <div class="mb-3">
            <img src="<%= article.imageUrl %>" alt="Article Image" class="img-fluid rounded" style="max-width: 100%; height: auto;">
        </div>
    <% } %>
            <p class="card-text"><%= article.article %></p>
        </div>
    </div>

    <div class="mt-3">
        <a href="/articles/showall" class="btn btn-primary">Back to Home</a>
    </div>

</body>
</html>
